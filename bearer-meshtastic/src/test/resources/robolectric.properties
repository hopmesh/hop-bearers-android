# The Meshtastic bearer's pure wire + state-machine tests are plain JVM (no Android framework), but the
# module is an Android library, so Robolectric supplies the runner for any test that touches android.jar.
# Pin the shadow SDK image to 33 (the same image the LAN bearer + driver suites use); minSdk is 29 and
# none of the exercised paths need a newer framework.
sdk=33
